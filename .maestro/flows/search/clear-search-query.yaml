appId: org.wikipedia
---
# Taps the search screen's clear ("x") button. Verified Android-only real
# resource id (2026-07-14) - not yet verified on iOS, so this flow is
# Android-scoped rather than guessed for both platforms.
- tapOn:
    id: "org.wikipedia:id/search_close_btn"

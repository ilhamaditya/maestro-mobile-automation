{
  "name": "maestro-automation-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Node/TypeScript orchestration layer for the Maestro mobile automation platform - env loading, test data factories, sample app fetching, report aggregation, and enforced flow conventions. Runs alongside, never inside, Maestro's own embedded JS engine.",
  "scripts": {
    "lint:flows": "tsx src/lint/check-flow-conventions.ts",
    "lint:naming": "tsx src/lint/check-naming-conventions.ts",
    "lint": "npm run lint:flows && npm run lint:naming",
    "fetch:apps": "tsx src/apps/fetch-sample-apps.ts",
    "smoke:android": "tsx src/cli/run-smoke.ts --platform android --tags smoke",
    "smoke:ios": "tsx src/cli/run-smoke.ts --platform ios --tags smoke",
    "report:aggregate": "tsx src/reporting/aggregate-reports.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.14.0",
    "dotenv": "^16.4.5",
    "execa": "^9.3.0",
    "fast-glob": "^3.3.2",
    "js-yaml": "^4.1.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4",
    "vitest": "^3.2.7"
  }
}

# Copy this file to `.env.dev` (gitignored) to customize dev-target runs.
# See config/environments/.env.local.example for a fully-commented reference
# and config/README.md / docs/EnvironmentGuide.md for how this is loaded.

ENVIRONMENT=dev

APP_ID_ANDROID=org.wikipedia
APP_ID_IOS=org.wikimedia.wikipedia
SEARCH_QUERY=Software testing

# --- Placeholders for Phase 2 (not read by any Phase 1 code) ---
# API_BASE_URL=
# TEST_USER_EMAIL=
# TEST_USER_PASSWORD=

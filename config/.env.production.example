# Copy this file to `.env.production` (gitignored) to customize
# production-target runs. See config/.env.local.example for a
# fully-commented reference and config/README.md / docs/EnvironmentGuide.md
# for how this is loaded.
#
# IMPORTANT: only flows tagged `production-safe` may ever target this
# environment - see docs/EnvironmentGuide.md for the enforcement plan.

ENVIRONMENT=production

APP_ID_ANDROID=org.wikipedia
APP_ID_IOS=org.wikimedia.wikipedia
SEARCH_QUERY=Software testing

# --- Placeholders for Phase 2 (not read by any Phase 1 code) ---
# API_BASE_URL=
# TEST_USER_EMAIL=
# TEST_USER_PASSWORD=
